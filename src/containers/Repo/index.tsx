import React from 'react';
import styles from './Repo.module.scss';

const Repo: React.FC = () => {
  return (
    <div className={styles.container}>
      <h1>Repo</h1>
    </div>
  );
};

export default Repo;
